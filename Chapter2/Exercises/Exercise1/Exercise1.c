#include <stdio.h>

int main(void) {
  printf("hello, world\n");

  // I'm not getting warning message from the compiler
  // But, if I get warning message from the compiler
  // I think this is needed to make it go away
  return 0;
}
